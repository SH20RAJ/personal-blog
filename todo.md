add tweets feature - to post little updates and things like shayri, quotes, jokes,  etc. with just a textarea and a post button
add it to db schema and it will share common category/tags/others with posts so that if we want to fetch all posts and tweets we can do it easily
and in feeds we will see tweets and posts mixed
tweets will have a different icon and color and 
tweet will have a title(very optional) and tweet in the schema.

add a search bar to search tweets and posts